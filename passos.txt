git config --global user.name "LSeraphim"
git config --global user.email "leostart133@gmail.com"

dotnet new mvc -o Pokedex
cd Pokedex

dotnet watch run

ctrl + shift + p

pacotes:
    Microsoft.AspNetCore.Identity.EntityFrameworkCore
    Microsoft.EntityFrameworkCore
    Microsoft.EntityFrameworkCore.Design
    Microsoft.EntityFrameworkCore.Tools
    Pomelo.EntityFrameworkCore.MySql


Ferramentas: 
    dotnet ef | dotnet tool install --global dotnet-ef
    dotnet tool update --global dotnet-ef --version 8.0.7

    dotnet ef add migrations inicializando-banco

    dotnet ef database update